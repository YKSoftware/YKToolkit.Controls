<UserControl x:Class="YKToolkit.Sample.Views.TextBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:YKToolkit.Sample.ViewModels"
             d:DataContext="{d:DesignInstance vm:TextBoxViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="20,0">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="4" />
            <RowDefinition />
            <RowDefinition Height="4" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="Sample text." />
        <TextBox Grid.Row="2" Text="Read-Only text." IsReadOnly="True" />
        <TextBox Grid.Row="4" Text="Disabled input." IsEnabled="False" />
    </Grid>
</UserControl>
